x-veld:
  code:

services:
  veld_transform_wiki_json_to_txt:
    build: .
    volumes:
      - ./src/:/veld/code/:z
    command: python /veld/code/transform_wiki_json_to_txt.py
    environment:
      out_txt_file: null

